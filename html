<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pass Your License - Sign In</title>
    <!-- Google Fonts: Plus Jakarta Sans for a modern, geometric look -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        /* --- Reset & Base Styles --- */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: #020617; /* Very Dark Navy */
            color: #ffffff;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            position: relative;
        }

        /* --- Background Effects (The "High-End" Feel) --- */
        
        /* 1. The subtle medical watermark pattern */
        .bg-pattern {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: radial-gradient(#1e293b 1px, transparent 1px);
            background-size: 30px 30px;
            opacity: 0.1;
            z-index: 0;
        }

        /* 2. Ambient Glow Orbs (Lighting) */
        .orb {
            position: absolute;
            border-radius: 50%;
            filter: blur(80px);
            z-index: 1;
            opacity: 0.4;
            animation: float 10s infinite ease-in-out;
        }

        .orb-1 {
            width: 300px;
            height: 300px;
            background: #0ea5e9; /* Sky Blue */
            top: -50px;
            left: -50px;
        }

        .orb-2 {
            width: 400px;
            height: 400px;
            background: #2563eb; /* Darker Blue */
            bottom: -100px;
            right: -100px;
            animation-delay: -5s;
        }

        /* 3. Floating Medical Icons (Watermark) */
        .medical-icon-bg {
            position: absolute;
            color: #38bdf8;
            opacity: 0.03; /* Very subtle */
            z-index: 0;
            font-size: 8rem;
            pointer-events: none;
        }

        .icon-pos-1 { top: 10%; left: 10%; transform: rotate(-15deg); }
        .icon-pos-2 { bottom: 15%; right: 10%; transform: rotate(15deg); }
        .icon-pos-3 { top: 40%; right: 20%; font-size: 4rem; opacity: 0.02; }

        @keyframes float {
            0%, 100% { transform: translate(0, 0); }
            50% { transform: translate(20px, 40px); }
        }

        /* --- Login Card (Glassmorphism) --- */
        .login-card {
            position: relative;
            z-index: 10;
            background: rgba(15, 23, 42, 0.6); /* Semi-transparent Slate */
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-top: 1px solid rgba(255, 255, 255, 0.15); /* Highlight top border */
            padding: 3rem 2.5rem;
            border-radius: 24px;
            width: 100%;
            max-width: 420px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            margin: 1rem;
            animation: slideUp 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* --- Header Section --- */
        .header {
            text-align: center;
            margin-bottom: 2.5rem;
        }

        .logo-container {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            margin-bottom: 1rem;
        }

        .logo-icon {
            font-size: 2rem;
            color: #38bdf8; /* Light Blue */
            filter: drop-shadow(0 0 8px rgba(56, 189, 248, 0.5)); /* Logo Glow */
            animation: pulse-heart 2s infinite;
        }

        @keyframes pulse-heart {
            0% { transform: scale(1); }
            15% { transform: scale(1.1); }
            30% { transform: scale(1); }
            100% { transform: scale(1); }
        }

        .logo-text {
            font-size: 1.25rem;
            font-weight: 700;
            color: #fff;
            letter-spacing: -0.5px;
        }

        .welcome-text {
            font-size: 1.75rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            background: linear-gradient(to right, #ffffff, #94a3b8);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .sub-text {
            color: #94a3b8;
            font-size: 0.9rem;
        }

        /* --- Form Elements --- */
        .form-group {
            margin-bottom: 1.5rem;
            position: relative;
        }

        .form-label {
            display: block;
            font-size: 0.85rem;
            font-weight: 500;
            color: #cbd5e1;
            margin-bottom: 0.5rem;
            margin-left: 4px;
        }

        .input-wrapper {
            position: relative;
        }

        .input-icon {
            position: absolute;
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
            color: #64748b;
            font-size: 1rem;
            transition: color 0.3s ease;
        }

        .form-input {
            width: 100%;
            padding: 14px 16px 14px 44px; /* Left padding for icon */
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            color: #fff;
            font-size: 1rem;
            font-family: inherit;
            outline: none;
            transition: all 0.3s ease;
        }

        .form-input::placeholder {
            color: #475569;
        }

        /* Input Focus State (Glow Effect) */
        .form-input:focus {
            background: rgba(56, 189, 248, 0.05);
            border-color: #38bdf8;
            box-shadow: 0 0 15px rgba(56, 189, 248, 0.15);
        }

        .form-input:focus + .input-icon {
            color: #38bdf8;
        }

        /* --- Button (The Star of the Show) --- */
        .btn-primary {
            width: 100%;
            padding: 16px;
            margin-top: 1rem;
            background: linear-gradient(135deg, #0ea5e9, #2563eb);
            color: white;
            font-size: 1rem;
            font-weight: 600;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
            /* The Neon Shadow */
            box-shadow: 0 4px 20px rgba(14, 165, 233, 0.4); 
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 25px rgba(14, 165, 233, 0.6);
            background: linear-gradient(135deg, #38bdf8, #3b82f6);
        }

        .btn-primary:active {
            transform: translateY(0);
        }

        /* Button shine effect */
        .btn-primary::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: 0.5s;
        }

        .btn-primary:hover::after {
            left: 100%;
        }

        /* --- Footer Links --- */
        .footer-links {
            margin-top: 1.5rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.8rem;
        }

        .link {
            color: #94a3b8;
            text-decoration: none;
            font-size: 0.9rem;
            transition: color 0.3s;
        }

        .link:hover {
            color: #38bdf8;
        }

        .signup-text {
            color: #cbd5e1;
            font-size: 0.9rem;
        }

        .signup-link {
            color: #38bdf8;
            font-weight: 600;
            text-decoration: none;
            margin-left: 5px;
        }
        
        .signup-link:hover {
            text-decoration: underline;
        }

        /* Mobile Adjustments */
        @media (max-width: 480px) {
            .login-card {
                padding: 2rem 1.5rem;
                border-radius: 20px;
            }
            .welcome-text {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>

    <!-- Background Decoration Elements -->
    <div class="bg-pattern"></div>
    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>

    <!-- Floating Medical Icons (Watermark Effect) -->
    <i class="fa-solid fa-stethoscope medical-icon-bg icon-pos-1"></i>
    <i class="fa-solid fa-user-doctor medical-icon-bg icon-pos-2"></i>
    <i class="fa-solid fa-notes-medical medical-icon-bg icon-pos-3"></i>

    <!-- Main Card -->
    <div class="login-card">
        
        <!-- Header & Logo -->
        <div class="header">
            <div class="logo-container">
                <i class="fa-solid fa-heart-pulse logo-icon"></i>
                <span class="logo-text">Pass Your License</span>
            </div>
            <h1 class="welcome-text">Welcome Back</h1>
            <p class="sub-text">Please enter your details to sign in.</p>
        </div>

        <!-- Form -->
        <form action="#" method="POST">
            <!-- Email Input -->
            <div class="form-group">
                <label for="email" class="form-label">Email Address</label>
                <div class="input-wrapper">
                    <input type="email" id="email" class="form-input" placeholder="nurse@example.com" required>
                    <i class="fa-regular fa-envelope input-icon"></i>
                </div>
            </div>

            <!-- Password Input -->
            <div class="form-group">
                <label for="password" class="form-label">Password</label>
                <div class="input-wrapper">
                    <input type="password" id="password" class="form-input" placeholder="••••••••" required>
                    <i class="fa-solid fa-lock input-icon"></i>
                </div>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="btn-primary">
                Sign In
            </button>

            <!-- Footer Links -->
            <div class="footer-links">
                <a href="#" class="link">Forgot Password?</a>
                <p class="signup-text">
                    Don't have an account? 
                    <a href="#" class="signup-link">Sign up</a>
                </p>
            </div>
        </form>
    </div>

</body>
</html>
